/**
 * Interrupteurs fonctionnels (réactiver en passant à true si besoin).
 */

/** Bloc « Indicateurs clés » (KPI) sur la fiche commune + en-têtes KPI à l’impression PDF */
export const SHOW_COMMUNE_KEY_INDICATORS = true

/** Export PDF depuis l’interface (backend prêt, UI à activer quand la fonctionnalité sera livrée) */
export const ENABLE_PDF_EXPORT = false

/** Popups et interactions clic/survol sur la carte (carte purement visuelle si false) */
export const ENABLE_MAP_POPUPS = false
